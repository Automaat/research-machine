# Markdown linting rules
# https://github.com/DavidAnson/markdownlint/blob/main/doc/Rules.md

# Default state for all rules
default: true

# MD013 - Line length
# Disabled: prose wrapping is cosmetic; most tools reflow automatically
MD013: false

# MD024 - Multiple headings with same content
MD024:
  # Allow duplicate headings in different sections
  siblings_only: true

# MD033 - Inline HTML
# Disabled: skill files use semantic XML-style tags (<example>, <body>, etc.)
MD033: false

# MD041 - First line in file should be top-level heading
MD041: false

# MD046 - Code block style
MD046:
  # Prefer fenced code blocks
  style: fenced

# MD049 - Emphasis style
MD049:
  # Prefer asterisks for emphasis
  style: asterisk

# MD050 - Strong style
MD050:
  # Prefer asterisks for strong
  style: asterisk

# MD036 - Emphasis used instead of heading
MD036: false  # Allow emphasis for section labels (intentional styling)

# MD028 - Blank line inside blockquote
MD028: false  # Allow blank lines in blockquotes for readability

# MD029 - Ordered list item prefix
MD029: false  # Allow any numbering style (1. 2. 3. or 1. 1. 1.)

# MD060 - Table column style
MD060: false  # Disable table alignment checks (cosmetic)

# MD025 - Multiple top-level headings
MD025: false  # Allow multiple h1s for templates/long research docs

# MD040 - Fenced code language
MD040: false  # Don't require language tags on every code fence in notes

# MD055 - Table pipe style
MD055: false  # Allow github-style tables without leading/trailing pipes

# MD056 - Table column count
MD056: false  # Tolerate uneven cells in informal research tables
